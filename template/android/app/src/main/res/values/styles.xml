<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:textColor">#000000</item>
        <item name="android:datePickerDialogTheme">@style/MyDialogTheme</item>
        <item name="android:timePickerDialogTheme">@style/MyDialogTheme</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:navigationBarColor">#fff</item>
        <item name="android:navigationBarDividerColor">#0fff</item>
    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">#0B3039</item>
    </style>
</resources>
